const USER_AUTH = 'USER_AUTH';
const USER_INFO = 'USER_INFO';
const USER_LOCALE = 'USER_LOCALE';
const USER_LOGOUT = 'USER_LOGOUT';

const userTypes = {
  USER_AUTH,
  USER_INFO,
  USER_LOCALE,
  USER_LOGOUT
};

export { userTypes as default, userTypes, USER_AUTH, USER_INFO, USER_LOCALE, USER_LOGOUT };
